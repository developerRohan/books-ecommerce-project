<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>
<h2>Your Cart</h2>
<%= render 'line_item',obj: @cart %>
  
<%= button_to 'Empty cart', @cart, method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to "continue shopping", store_path , class:"btn btn-xs btn-primary"%>